import 'package:flutter/material.dart';

class MainContainerBackground extends StatelessWidget {
  const MainContainerBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
